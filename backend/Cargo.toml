[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
frontend = { path = "../frontend" }
syn = { version = "2.0", features = ["full", "extra-traits"] }
graph-cycles = "0.3.0"
petgraph = "0.8"
easy-smt = "0.2.2"
quote = "1.0"
